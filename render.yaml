services:
  - type: web
    name: life-os-private-practical-co-pilot
    env: python
    plan: free
    runtime: python-3.11.9
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn lifeos.main:app --host 0.0.0.0 --port $PORT
