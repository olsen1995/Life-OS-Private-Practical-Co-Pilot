name: Manifest Hygiene Check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  hygiene:
    name: Run Manifest Hygiene
    runs-on: windows-latest

    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v3

      - name: 🧼 Run Manifest Hygiene Script
        shell: pwsh
        run: |
  if (Test-Path "./scripts/check/manifest_hygiene.ps1") {
    Write-Host "✅ Running manifest hygiene check..."
    ./scripts/check/manifest_hygiene.ps1
  } else {
    Write-Host "⚠️ manifest_hygiene.ps1 script not found, skipping."
  }
