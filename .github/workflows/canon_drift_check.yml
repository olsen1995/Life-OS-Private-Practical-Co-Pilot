name: Canon Drift Check

on:
  push:
    paths:
      - "lifeos/canon/**"
      - "tests/canon/test_drift.py"
  workflow_dispatch:

jobs:
  drift_check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest

      - name: Run drift check
        run: |
          pytest tests/canon/test_drift.py
