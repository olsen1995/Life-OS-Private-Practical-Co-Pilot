name: Canon Metrics Invariants

on:
  push:
    paths:
      - "lifeos/canon/**"
      - "tests/canon/test_metrics_invariants.py"
  workflow_dispatch:

jobs:
  metrics_invariants:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest

      - name: Run metrics invariants test
        run: |
          pytest tests/canon/test_metrics_invariants.py
